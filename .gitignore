# Dependencias (si se agregan en fases futuras)
node_modules/
npm-debug.log*

# Artefacto de npm init generado al instalar Playwright para tests locales
package.json
package-lock.json

# Archivos de sistema
.DS_Store
Thumbs.db

# Variables de entorno (NUNCA en el repo)
.env
.env.local

# Builds empaquetados
*.zip
*.crx
*.apk

# IDEs
.vscode/
.idea/
*.swp

# Archivos de prueba con datos reales
*.vault.json
test-credentials.*settings.local.json
settings.local.json
CLAUDE.md
!.claude/CLAUDE.md
.wrangler/

# Keystore de firma Android (NUNCA en el repo)
*.keystore
*.jks

# Artefactos de compilación Android (generados por Gradle — no en el repo)
app/build/
.gradle/
build/
local.properties
