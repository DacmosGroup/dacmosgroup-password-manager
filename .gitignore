# Dependencias (si se agregan en fases futuras)
node_modules/
npm-debug.log*

# package.json y package-lock.json se rastrean desde v0.6.0 (Capacitor deps)

# Archivos de sistema
.DS_Store
Thumbs.db

# Variables de entorno (NUNCA en el repo)
.env
.env.local

# Builds empaquetados
*.zip
*.crx
*.apk

# IDEs
.vscode/
.idea/
*.swp

# Archivos de prueba con datos reales
*.vault.json
test-credentials.*settings.local.json
settings.local.json
CLAUDE.md
!.claude/CLAUDE.md
.wrangler/

# Keystore de firma Android (NUNCA en el repo)
*.keystore
*.jks

# Artefactos de compilación Android/iOS (generados — no en el repo)
app/build/
android/app/build/
android/.gradle/
android/build/
android/local.properties
.gradle/
build/
local.properties

# iOS — CocoaPods y Swift PM build cache
ios/App/Pods/
ios/.build/
ios/App/.build/

# Documentos de sesión Claude Code (planificación interna, no doc formal)
docs/auditoria-remediaciones-*.md
